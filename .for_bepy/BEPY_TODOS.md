# Manual tasks for Joe

### Urgent

- Capture `[perm-relay]` logs for the broken AskUserQuestion card (ai_todo 16, needs v0.1.65+): run `cargo tauri dev`, open a chat, trigger an AskUserQuestion (or just have a turn ask you a question), then grep the daemon + app logs and the webview console for `[perm-relay]`. Paste them back - the table in ai_todo 16 maps the log pattern to the exact dead hop so the fix becomes one-shot. (Can't be Claude-tested: GUI app + the sandbox blocks daemon spawn.)
- Relaunch the Claude Usage tray app - I killed it during the AFK e2e run (the wdio harness kept nuking its daemon); it's currently down so usage tracking is off.
- Get a dev port for claude_usage from server_supervisor's allocator, then tell me to apply it (ai_todo 78). Until then claude_usage still defaults to 1420.

- Startup hardening smoke test (ai_todo 24, needs relaunch): kill BOTH tray app + daemon, then relaunch just the app. Confirm tray appears and a fresh chat works on the first try - no "clear and run again" ritual. Then kill only the daemon while the app is running; confirm a "Daemon disconnected - reconnecting..." toast fires, followed by "Daemon reconnected." once the daemon is back up (app auto-respawns it).

### Visual QA

- New-session model picker (needs `cargo tauri dev` rebuild - new Rust IPC): open the picker. Labels should read "Haiku / Sonnet / Opus / Fable" (pretty, not raw ids), sorted least-to-most impressive left-to-right. Select Fable - "Start session" should disable and a red message "Fable 5 is disabled, please choose another model" should appear (Fable is currently off on the account). Selecting any other model re-enables Start. (Until the app is rebuilt the probe IPC is missing, so it fails open and nothing disables.)
- New project button (needs relaunch): in the project picker footer, click "New project..." - the picker should HIDE and the new-project modal should appear (not stack on top). Type a name - cursor must not jump or reverse. Pick a folder; preview updates live. Hit Create - folder is created and the flow continues. Cancel/Esc dismiss without creating. Clicking "New project..." again while the modal is open must be a no-op (no second modal).

- Transcript tool-chip strip (ai_todo 44, 2026-06-11, commit ae0e51c, NEEDS RELAUNCH - frontend only). A prior agent ran out of context mid-build; the WIP was complete + unit-tested (277 vitest green, tsc clean), just uncommitted, so it's now committed. Run a turn that does several Reads/Greps/Bashes/Edits and confirm: (1) ONE inline row of chips per turn (`Read x4 | Grep x1 | Ran x9`), NOT a vertical stack, and the row does NOT break apart when assistant text is interleaved. (2) Click a chip - its calls expand in a panel BELOW the row; click another chip - it switches (single-open, the previous hides); click the active chip again - it closes. (3) Rich inline edit/diff cards still render in-flow (NOT folded into a chip). (4) HIGHLIGHT: while a turn runs, the chip whose count just incremented (or a brand-new chip) briefly flashes. (5) RELOAD: leave the chat and come back (or switch away/back) - the chips must PERSIST as a strip, not split back into separate rows (this was the original reload bug; now covered by a red->green jsdom test). Unit-tested at the DOM level; the live look/flash/accordion needs your eyes. NOTE: the "Edited" chip lists each edit call's row (file target per row); a richer per-file aggregate count was left out as optional - tell Claude if you want it.
- Composer fixes (new, NEEDS RELAUNCH - frontend only). (1) NEWLINES: type a multi-line message with Shift+Enter, send it - your bubble should keep the line breaks now (no longer collapsed to one line). Confirm Claude's replies still paragraph normally (only your messages got forced breaks). (2) SLASH HIGHLIGHT: in the composer, type a real command like `/commit` - it should color live as you type (skills purple, builtins blue, commands teal, matching the sent message); type `/asdasdasd` and it stays plain text. WATCH FOR: glyph misalignment between the colored backdrop and the caret/text (the highlight is a backdrop behind a transparent-text textarea) - especially with long wrapped lines or when the textarea scrolls past 200px. If text sits off from the caret, tell Claude to tune the backdrop metrics.
- Tool activity grouping (new, NEEDS RELAUNCH - frontend only). (1) CHAT BODY: run a turn that does several Reads/Greps/Bashes - the body should now show ONE collapsible counter per tool type ("Grep x5", "Read x2") that increments live as calls land, not a row per call; click a counter to expand and see every call of that type. Rich edit/diff cards still render inline (NOT folded). (2) STATUSBAR CHIPS: each chip (Grep/Read/Ran/...) now opens its OWN drop-down listing that tool's targets (files open in editor, images lightbox, Grep/Bash show the pattern/command). The dropdown opens DOWNWARD off the chip. (3) SETTINGS: Settings > Statusline > "Tool activity chips" - untick a tool (AskUserQuestion + Todo updates are hidden by default) and confirm its chip disappears after reopening a chat; "Reset to defaults" restores it. Unit-tested at the DOM level (vitest); the live look/increment needs your eyes.
- Murky / Sgt Hammer / Gall (2026-06-06, extracted from local HotS CASC): Characters > Refresh, listen - they now have full kits (16/16/18 clips). Murky + Sgt Hammer confirmed good by Joe.
- Cartoons (Family Guy / SpongeBob / Simpsons) DROPPED 2026-06-06: removed entirely (mislabeled + low-bitrate fan-soundboard audio, no clean local game source). To revisit: install a game (SpongeBob: Battle for Bikini Bottom Rehydrated on Steam; Simpsons: Hit & Run) and tell Claude - storm-extract/CASC-style extraction is now proven and would pull clean VO.
- Re-sourced clips (autopilot 2026-06-06, data in %APPDATA%\claude-usage-tauri\characters\, NOT git - needs app relaunch + Refresh): 1054/1136 cropped clips replaced with FULL-length lines. (1) Play a few HotS clips (alarak, abathur, sonya), a sims clip, an acolyte (wc3) clip - should be full lines, not 2s mid-word chops. (2) Audition the 113 clips in `.for_bepy/resource_pending_5to10s.txt` (5-10s band) - if any feels too long for a chime, tell Claude "find a shorter <char> <action> line". (3) DECISION: 82 clips left as the old crop (`.for_bepy/resource_unresolved.txt`, ai_todo 33) - mostly Stewie DVD-only lines + non-descriptive simpsons filenames + army-men hero VO the PC release ships no audio for. Want a looser-phrase family-guy pass (~6-8 more recoverable) or accept the crops?
- Context-window % single source of truth (ai_todo 30, NEEDS RELAUNCH - new Rust + frontend + global skills): after relaunch, (1) open a chat and confirm the context % chip shows a sensible number and NO longer wrongly pins 100%; a heuristic-window value shows a leading "~" and "(estimated)" in the tooltip. (2) In a claude session (terminal or in-app), run /context-left or ask "how much context do I have left" - it should answer from the daemon (no "computed locally" note); kill the app and confirm it falls back to "computed locally". (3) On your PHONE via remote control, ask "how much context left" - the agent replies in-chat with the %. (4) Autopilot now self-regulates: at ~50% used it slows down, at ~60% used it hard-stops and writes remaining work to ai_todos. KNOWN CEILING: there is NO definitive Opus 200K-vs-1M signal anywhere (checked transcript init line, parser, SSE), so the window stays a heuristic plus a sticky "any turn over 200K proves >=1M" correction - that is why some numbers show "~". The /context-left and autopilot skills are global (~/.claude), not in this repo.
- Pipe frame-size warning downgrade (ai_todo 25, DONE - needs relaunch + a real update transition to confirm): after the next app update/restart, watch daemon.log. The old `transport_windows: frame size N exceeds 16777216` WARN should no longer appear at warn level. If an old pre-frame binary ever connects again, you'll instead see a DEBUG line including the client PID (so you can correlate which binary did it). Root cause was confirmed harmless (an old pre-frame-protocol app binary writing unframed JSON during an update; daemon drops the frame and survives). Code-complete + build-clean; only the live log behavior needs your eyes.
- Heroes of the Storm characters (ai_todo 27, DONE by autopilot - data lives in %APPDATA%\claude-usage-tauri\characters\heroes-of-the-storm\, NOT in git): open sidemenu > Characters > Refresh and confirm a `HEROES OF THE STORM` group appears with 90 heroes, each with a portrait icon. Spot-check sounds on a few (assign one to a project and trigger a ping, or use the slot preview): the-butcher ("Fresh meat!"), illidan, abathur, deckard-cain, etc. Each hero's `sounds/` folder is a POOL (all ripped clips, ~1265 total) - the slot mapping only uses a subset, so if a hero's auto-picked slot sound is weak, remap it from the pool in the UI. KNOWN GAPS: mostly RESOLVED 2026-06-06 - murky, sgt-hammer (codename `SiegeTank`), and gall were re-extracted as full kits (~16-18 clips each, 5-6/6 slots) straight from the local game CASC via storm-extract, since no online rip carried them. Remaining minor: arthas/nazeebo thin on auto-slotted lines (pool clips exist to remap); 7 HotS "laugh" clips still cropped.
- Sleep/Shutdown-when-done protocol (ai_todos 28+29, NEEDS RELAUNCH - new Rust + frontend): open the chat window, click the new top-right more-options (⋮) button - confirm New chat / History / Sort-by all still work from inside the menu (they were moved in). Arm "Sleep when done": the ⋮ button + the menu item should show an amber armed dot. Start a couple of chats, let them finish; confirm the protocol injects `/close` into each, then a cancellable ~30s countdown chip appears ("Sleep in Ns" + Cancel) - hit Cancel and confirm it disarms. Then let it run to 0 and confirm the PC actually sleeps (and separately, "Shutdown when done" actually shuts down - test that LAST). While armed, trigger a permission/AskUserQuestion in one chat and confirm the protocol auto-resolves it (auto-accepts / auto-answers first option) rather than hanging, and that the auto-answer is logged. Only Claude-testable up to the unit level (logic is unit-tested, 15 frontend + 3 backend cases); the real close->countdown->sleep/shutdown chain + the menu's visual layout need your eyes. TASTE REVIEW: the menu order/copy/icons (ph-moon-stars sleep, ph-power shutdown) were Claude's guess - tweak if you don't like them. KNOWN LIMITATION: the engine's auto-resolution log writes to COMMENTS_FOR_BEPY.md via a compile-time repo path (fine in dev, would need an app-data path for a packaged release).
- Sidebar close animation flash-back (REAL fix - supersedes the 1d4ed9c/8f28977/99db7bd attempts): open a chat, then close it via the row's ⋮ > Close. Confirm the closed row slides left, the rows below slide up, and the closed slot does NOT briefly snap/flash back before settling. Root cause was timing, not the `nodes` array: the exiting row was removed on its own `animationend` (~280ms), reflowing siblings up BEFORE `applyReorder`'s FLIP (~310ms) yanked them back down for one frame using stale positions. Fix makes `applyReorder` the sole remover so removal + slide-up happen in one synchronized step. Deterministic regression test added (`tests/sidebar-anim-close.test.mjs`); the sub-300ms paint frame itself can only be confirmed by eye. FOLLOW-ON: also confirm that opening a NEW chat right after closing one shows the new draft row immediately (it no longer waits until you type a message). That was a starvation side effect of the flash fix; the deferred reorder now uses a fixed deadline so a burst of reconciles can't push it back. SHADOW: when a group of rows slides up, only the BOTTOM-MOST rising row should cast the drop-shadow now (previously each rising row stacked its shadow on the row below). Confirm it looks like one clean shadow under the moving group.

- AI auto-chat-titles (new, needs relaunch - new Rust): start a fresh in-app chat, send one message; within ~5s the sidebar row title should become a concise AI-generated title (not the raw first prompt). Keep chatting: at ~5 and ~15 messages the title refines again (it only re-titles at messages 1/5/15, never every turn). Confirm the `<cc-title:...>` marker NEVER appears in the message bubble (should be stripped like the done/question marker). Then `/close`-rename the chat and keep chatting: the AI must NOT overwrite your name (human rename is sticky). Title is read off the transcript; Claude emits `<cc-title:..>` via --append-system-prompt.
- Meeting-mode notification pause (new): join a real Teams/Zoom/Discord call (or just turn your camera/mic on), trigger a work-finished/question ping, and confirm NO sound/voice fires while the call is live. Hover the tray icon: it should show an "In a meeting - notifications paused" line. End the call: pings resume within ~3s. Toggle is Settings > Notifications > Meetings (default ON); flip it off and confirm pings fire even mid-call. Detection is registry + WASAPI, Windows-only - needs your real camera/mic, can't be Claude-tested. NOTE: requires relaunching the tray app (new Rust).
- Permission-on-backgrounded-chat (ai_todo 79): start a tool-heavy turn in chat A, switch to chat B; confirm A's sidebar row highlights + the shield icon pulses amber and sorts to the top. Click A: the permission/question card should surface and answering it lets the turn continue. (Gating/store logic is unit-tested; needs your eyes on the highlight + live answer.)
- Changes-panel checkbox (ai_todo 80): open a chat's all-changes rail with a long file list, scroll down, tick a "reviewed" checkbox; confirm the "N of M reviewed" chip updates without the list rebuilding or the scroll jumping. Opening a row's diff sheet should still work.
- Project-detail "+" new chat (needs relaunch): click the + on RUNNING INSTANCES header, confirm modal appears, picking a config opens/focuses the chats window with a fresh draft for that project (NOT a draft in the main window). Test with chats window both closed (fresh-create) and already open (focus + event).
- RELAUNCH to test Open-in-chats: the CTA fix touches Rust (new open_chats_for_session command), so the running tray app must be restarted before the CTA works - the live app is still on the old binary.
- Project-detail recent-chats overflow: confirm the recent-chats table no longer spills past the card edge - long chat names should ellipsis-truncate.
- Chat-detail revamp: open a CLOSED chat (from project-detail recent chats) - confirm the new cards render (title from history not the id, Date | Time row, Total tokens, Turns/Messages, token pie + legend, cache read/create/efficiency). Then open a LIVE chat (running instance) - confirm it shows the reduced set (overview + turns/messages, no pie/cache) and updates live.
- Chat-detail actions: the header ⋮ menu should Copy PID (live only) and Copy Session id; the "Open in chats" CTA should now OPEN/FOCUS the separate chats window (not switch the main window's route) - live chat selects in Sessions there, closed chat opens read-only in History there. Test with the chats window both closed (fresh-create + drain) and already open (focus + event).
- Recent-chats table: confirm the new When | Tokens | Chat order, chat name fills the remaining width + ellipsis-truncates, and tokens read in millions (e.g. 0.2M, 21.9M).
- Model/effort display (needs relaunch - new Rust): open a LIVE chat detail and a running-instance row; both should show its model (opus/sonnet/haiku) + effort (e.g. high/max). Start a NEW chat, then close it and open its detail - it should still show model + effort (effort comes from the new durable chat-config.json). NOTE: chats that were already CLOSED before this lands show model (from transcript) but NO effort - effort only persists for chats started from here on. The persist logic is unit-tested; this is just confirming the display.
- News AI summary (billed, NEEDS RELAUNCH - new Rust): open an un-summarized (old) post and confirm it now STREAMS live - "Fetching article…" → "Writing…" → Markdown writing in token-by-token with a blinking caret (runs at --effort low for speed). New posts auto-summarize eagerly in the background so they're instant on open. Confirm it reads well ("what shipped + why it matters"), renders headings/bold/bullets, ADHD-friendly small chunks. Header now: category | date ···· open-original ↗ on one line, title full-width below; Regenerate lives in the top-bar ⋮ (detail mode). Tinker `build_prompt` in `src-tauri/src/news/summarizer.rs` if the flavor/length is off.
- News e2e: run `npm run test:e2e:news` once the tray app is relaunched and port 1420 is ours (it nukes the daemon like other wdio runs, so do it when you can spare the tray app). It asserts the kebab menu + cached-summary detail render with no billed claude call.
- Paste-as-log chip (new): in a chat composer, paste a big chunk of text (2000+ chars). Confirm it does NOT fill the textarea but shows a "pasted_log.txt" chip in the attachments row; click it to view the full text in the lightbox, the x removes it. After SENDING, your sent message should ALSO show the chip (NOT the wall of text) - click it to view the full text in the lightbox; the full text still reaches Claude inline. Paste a second big chunk -> "pasted_log_2.txt".
- Long-message clamp (new): send (or paste-inline) a user message longer than ~10 lines. Confirm your bubble clamps with a bottom fade + "Show more" / "Show less" toggle; assistant replies are untouched.
- Turn-status icon (new, needs relaunch - new Rust): finish a normal turn in an in-app chat and confirm the sidebar shows a CALM muted check (not the old red exclamation), even after you read it. When Claude ends a turn by asking you something, confirm an AMBER chat-dots flag appears and that row sorts toward the top. Red is now reserved for real permission prompts only. (Relies on Claude emitting the <cc-status:..> marker via --append-system-prompt; the marker is stripped from display.)
- Background-chat amber flag (needs relaunch - new Rust): start TWO chats, switch away from chat A while it's running. When A's turn ends with a question, confirm the amber flag appears on A's row WITHOUT you ever opening A. Flag is now backend-driven via Instance.awaiting in the registry, not frontend-only.
- Sidebar exit-anim rewrite (ai_todo 17, position:absolute): close a chat via row ⋮ > Close and watch the row slide LEFT while the rows below slide UP into the freed space - confirm NO flash/snap-back of the closed slot before it settles. Then close-then-immediately-open a new chat: the new draft row must appear INSTANTLY (not wait for the close to settle or for you to type). When a group of rows rises together, only ONE clean drop-shadow should sit under the bottom-most rising row (not a stack). jsdom harness covers node lifecycle/keys; the sub-300ms paint can only be confirmed by eye.
- Three chat fixes (NEEDS RELAUNCH - new Rust + frontend). (1) AUTO-ACCEPT DOT: turn auto-accept ON for a chat, switch away, let it hit a tool permission - confirm NO "needs attention" shield dot appears on that row (it auto-allows in the background now instead of parking). Also: have a permission already parked (dot showing), THEN toggle auto-accept ON - the dot should clear immediately, not wait for a switch-away/back. NOTE auto-accept is still a frontend per-prompt auto-allow, NOT a real CLI permission mode (your call, kept by design). (2) CHANGES PANEL: after Claude makes edits in a chat, open the changes (changes-btn) rail - it should now LIST the edited files with +/- counts and tool rows should appear inline in the chat (parser was dropping tool_use blocks; now synthesized in both live + history). (3) INTERRUPT - LOAD-BEARING, can't be Claude-tested: while Claude is mid-turn, hit Stop/interrupt. Confirm the chat pane STAYS OPEN (no longer vanishes), and your next message CONTINUES the same session with no respawn lag. This relies on `claude -p --input-format=stream-json` honoring a `{"type":"control_request","request":{"subtype":"interrupt"}}` line on stdin instead of the old kill-the-process. If the turn does NOT actually stop when you hit interrupt, the CLI doesn't support that control message - tell Claude and we fall back to the kill+keep-pane-open approach.
- Four small chat fixes (2026-06-09, NEEDS RELAUNCH - new Rust + frontend). (1) CANCEL-TURN: after a turn finishes in a new chat, confirm the header "Cancel turn" (x) button is HIDDEN while you draft the next message, and only reappears mid-turn. (2) SLIDER COLOR: open the new-session model/effort modal - the two sliders should now render in the accent color, not the browser-default blue. (3) TITLE TAIL: confirm sidebar chat titles no longer end with a stray `</cc-title` (the Rust title parser now strips the XML-close variant). (4) AUTO-ALLOW: the new-session modal now has a checkbox "Auto allow permissions" checked by default - start a chat with it on and confirm tool prompts auto-allow without the modal (the header shield shows is-on); uncheck it and confirm prompts surface normally.
- Stale unit test (pre-existing, NOT from the 2026-06-09 fixes): `tests/session-statusbar-context.test.mjs` still expects `~45%` but commit b8a01b5 deliberately dropped the `~` prefix - update the test to match (or restore the prefix). It's the only red test in `pnpm test`.
- Remote-chat + configurable new-session modal (2026-06-10, NEEDS REBUILD+RELAUNCH - new Rust + frontend). LOAD-BEARING / can't be Claude-tested: (1) REMOTE CHAT: in the New-session modal, expand "More options" - confirm two checkboxes (Auto allow permissions, Remote chat), both default ON. Start a chat with Remote ON and confirm the chat's claude was spawned with `--remote-control` (it should appear in Claude desktop / be drivable from your phone). CRITICAL UNKNOWN: `--remote-control` is now passed ALONGSIDE the chat's `--input-format=stream-json` - this combination was never tested (Phase-5b seam). If the chat misbehaves (turns don't stream, daemon and phone fight over the process, or it won't start), the two modes conflict and we need to rethink - tell Claude. Start a chat with Remote OFF and confirm it's a normal chat (no bridge, no extra Claude desktop entry). (2) DEFAULTS IN SETTINGS: Settings > Session presets now has an editable comma-separated Models list + "Auto-allow by default" + "Remote chat by default" toggles - change them, save, open a new New-session modal and confirm the slider models + checkbox defaults reflect your settings. (3) DATA-DRIVEN MODELS: add a model id to the Models list (e.g. a new opus id), save, confirm it appears as a slider stop and is pickable in presets. Logic is unit-tested (cargo build + tsc + vitest green); the live --remote-control behavior + the modal/settings visuals need your eyes. ai_todo 40 = auto-querying available models (deferred, uncertain). Decisions + shas in COMMENTS_FOR_BEPY.md.
- Tool-call activity surfacing (2026-06-10 autopilot, NEEDS REBUILD+RELAUNCH - new Rust + frontend). After relaunch, in an in-app chat: (1) DOUBLING: send a message WITH an image/file attachment - confirm it shows ONCE, not twice (was doubling only on attachment messages). (2) COMPACT ROWS: each Read/Grep/Edit/Bash/Agent tool call now renders as a ONE-LINE collapsed row (icon + tool + target); click to expand the old JSON. Subagent (Agent) rows read "starting subagent" (not the giant prompt); long targets cap at 100 chars. (3) LIVE LABEL: while a turn runs, the bottom thinking bar shows the real action ("Reading x.ts", "Running: <desc>") instead of a random verb. (4) CCSTATUSLINE ROW: the chat's status bar shows a cumulative tally `Read x4 · Grep x3 · Edited x6 · Ran x5`; click it → Files|Media popover. Files tab → click a file opens it in VS Code; Media tab → images by filename + thumbnail, click → in-app lightbox. (5) SCROLL: scroll UP in a chat mid-turn - new messages should NOT yank you back to the bottom; only auto-scrolls if you were already at the bottom. Logic is unit-tested (tsc + vitest green); the visuals + the VS Code/lightbox opens need your eyes. Decisions + shas in COMMENTS_FOR_BEPY.md (top block).
- Changes-panel upgrade (2026-06-10, commits 5c9f4a3/d10f3ce/60a9f5e, frontend-only - vite/HMR or relaunch): (1) BADGE: after Claude edits files in a chat, the header git-diff button shows a small count badge (deduped file count, hidden at 0). (2) RAIL/SHEET THEME: open the changes rail + a file's diff sheet - they should now match the app palette (was hardcoded grays from phantom CSS vars); reviewed chip is a pill, checkboxes use the accent color. (3) DIFFS: expand an inline edit window (and open a sheet diff) - it now renders a UNIFIED diff: red/green line tints, +/- gutter, VS Code-grade syntax colors (Rust/TOML included now), and the exact changed WORDS get a stronger highlight in replaced line pairs. Hover a diff for a copy button (copies the post-edit text). Flip light/dark theme - token colors should flip too. Logic is unit-tested (14 diff-rows cases) + tsc/vitest/vite-build green; colors/readability need your eyes.
- Statusbar + chat fixes (2026-06-10, NEEDS RELAUNCH - frontend only). (1) STARTED-CHAT HEADER: start a NEW chat; once it begins, the top-right should show just the git-diff changes button + the ⋮ more-options menu (same as a reopened chat), NOT the old row of 5 icon buttons. Open ⋮ and confirm Auto-accept / Open in Terminal / Detach / Stop turn / Close session all work, and that the changes button's badge/panel work after Claude edits files. (2) MODEL/CTX/COUNTS ON OPEN: open a chat and confirm the model chip shows immediately (no skeleton-until-revisit), and msgs/turns are NOT the previous chat's values. (3) COMBINED CHIPS: run Bash AND PowerShell in a turn - the statusbar should show ONE "Ran" chip (not "Ran x4" + "Ran x8"); Edit/MultiEdit/NotebookEdit collapse into one "Edited" chip. Settings > Statusline tool list now shows canonical buckets (Read/Edited/Wrote/Ran/...). (4) DROPDOWN: click a statusbar tool chip - the drill-down is now fixed-width, long rows ellipsis (full text on hover), no horizontal scrollbar, and it never spills off-screen near the right edge. (5) CLOSE WITHOUT /close: close a chat from the sidebar row ⋮ > Close OR the in-chat ⋮ > Close session - no need to type /close. Unit-tested (tsc + 276 vitest green); visuals need your eyes. STILL TODO (ai_todos 44+45): transcript tool rows -> inline chip row + accordion + highlight-on-change; and the "1 msg" count / unsent-message-queue bug in that one broken chat.
- History performance (needs rebuild - new Rust + frontend, commits ae4d8d7 + 4e3e697): open History and confirm (1) a loading spinner shows in the session list while it loads, (2) the list comes up noticeably FASTER than before (backend now parses titles only for the visible page, not every transcript), (3) opening a past chat shows a "Loading transcript" overlay then renders, and you can SCROLL UP to load older messages (it now loads the last ~20 + older-on-scroll instead of the whole transcript), (4) reopening History is near-instant (title cache). Unit-tested (collect_history pagination/cache/filter); the speed + visuals need your eyes after a rebuild.
- New Sound settings page (2026-06-11, NEEDS RELAUNCH - new Rust + frontend, audio extracted to tauri_kit_audio). Settings now has a "Sound" row (under System, above Notifications). Open it: confirm (1) the output-device picker lists "System default" + your devices, the OS default is tagged "(current)", and picking a specific device makes sounds play through it (trigger a work-finished ping); (2) the "Pause sounds during meetings" toggle moved here from Notifications and still works; (3) Notifications no longer shows the Audio output / Meetings sections (mute toggles stayed). Build/tsc/vitest green; the live render + audio routing need your eyes.
- Subagent tool-call nesting -> 3-LEVEL drill-down (2026-06-12, commit b475f3d, supersedes the old 73ae5e8/98ca6fe 2-level nesting, NEEDS REBUILD+RELAUNCH - frontend only). LIVE CONFIRM NEEDED. Run a chat turn that dispatches one or MORE subagents (Task/Agent), each doing several Reads/Greps/Bashes. Confirm the drill-down is now: (1) main `Subagent xN` chip -> click -> a list of EACH subagent as its own chip, labeled by its DESCRIPTION (e.g. "Find subagent chip rendering code"), with a count of its tool calls; (2) click a subagent chip -> its tool calls re-chipped BY TYPE (Read xN / Grep xN / ...) like the normal strip; (3) click a tool-type chip -> the individual rows. The subagent's calls must NOT leak as loose rows in the main transcript, and must NOT inflate the main turn's chips. With multiple subagents, each one's tools stay isolated to its own chip. Same KNOWN LIMITATION as before: works LIVE + warm-reload (switch chats and back); a full app RESTART reads the on-disk transcript where subagent calls live in a separate subagents/ file, so they won't nest until a cold-reload-merge pass (deferred). Unit-tested at the DOM level (jsdom: 3-level nesting + multi-subagent isolation + main-strip-excludes-children + no-regression; tsc + vitest green).
- Tool-chips vanish on reload - FIXED (2026-06-12, commit 498c1a9, frontend-only, NEEDS a fresh in-app chat to confirm). Root cause: history mode emits one `turn_usage` per assistant line (`parser.rs:580`), and the renderer closed the turn on EACH one, orphaning every tool row after the first → flat rows on reopen. Now a turn is bounded by the next USER message; usage just settles the shimmer. CONFIRM: start a BRAND-NEW in-app chat (not an adopted terminal session - those render via the file-watcher and may never chip), run several tools, then leave the chat and come back - chips must PERSIST as ONE strip per turn, not collapse to flat rows. Unit-tested with a history-shape replay (288 vitest green); needs your eyes on a real reopen. NOTE: an *adopted external terminal session* showing flat rows is NOT this bug - test a native in-app chat.
- AUQ reliability - live confirm (2026-06-12, commit 3541e88, new Rust + frontend). After a real in-app AskUserQuestion: (1) go offline / close the app while a question is pending, reopen - the card should RE-SURFACE (poll resets `emitted` on reconnect). (2) Let one time out (now 1h) - the card should DISAPPEAR and you get an OS notification, and the model should be told "no answer yet, re-ask" NOT "dismissed". (3) Answer normally - card clears as before. Can't be Claude-tested.
- supervised-run can host this app? (2026-06-12): launching `cargo tauri dev` via supervised-run works to a window, but the daemon's `CREATE_BREAKAWAY_FROM_JOB` is DENIED (trapped in the supervisor's inherited job) AND the dev daemon seizes port 27182, which WEDGES other claude sessions' tool relay (Bash/PowerShell fail with `relay error ... /permissions/request`). Recovery: stop the supervised process. Real fix is an ai_todo (give supervised spawn the breakaway flag). Until then, run `! cargo tauri dev` in a normal terminal.
- Rate-limit banner + warning suppression (2026-06-12, commits 0dc7f18 + c26d3b2 + 631ed70, NEEDS REBUILD+RELAUNCH - new Rust + frontend). Hard to trigger on demand (needs a real account rate-limit rejection). When it happens, confirm: (1) the old `allowed_warning` JSON blob no longer dumps into the chat transcript (warnings fully suppressed); (2) a GLOBAL banner appears at the top of the Chats window naming the limit ("5-hour limit reached" / "Weekly limit reached"), with the exact reset time and a live countdown beneath it; (3) an "Auto continue on reset" checkbox (default CHECKED) - when the countdown hits zero with it checked, every chat that got interrupted receives a "continue" message automatically; uncheck it and confirm nothing auto-sends, the banner just clears at reset. LOAD-BEARING / can't be Claude-tested: the actual auto-resend on a real reset (esp. that "continue" lands cleanly without a duplicate user bubble, and that multi-session continue works). Banner logic is unit-tested (6 vitest cases: activate-on-rejected-only, type labels, multi-session, reset-fires-continue, auto-off-sends-nothing). Also: AUQ/permission prompt timeout extended 300s -> 1h so AFK answers don't expire (verify a prompt now waits much longer before giving up).
- Tool-chip strip splits on reload (2026-06-11, FIXED a47651c, NEEDS RELAUNCH - frontend only). Open a chat and run a turn with MANY tools (5+ Reads/Greps/Bashes so the turn exceeds ~8 events), let it finish; the live chip strip looks right. Switch to another chat and back: the strip must stay ONE strip with the full counts (e.g. `Bash x5`), NOT split into a second strip / loose rows further down the response. Root cause: a turn straddling the bulk-load CHUNK=8 boundary got grouped twice (a fresh map spawned a 2nd strip); applyTurnCollapse now recovers the existing strip from the DOM and extends it. Red->green jsdom regression locks it at the DOM level (278 vitest green); the live look needs your eyes.
- Follow-OS-default audio (2026-06-11, NEEDS RELAUNCH, can't be Claude-tested - needs a real device switch). With the Sound output device left on "System default", play a ping (works), then change Windows' default output device (Sound settings, or the volume-flyout device switcher) to a DIFFERENT device while the app stays open. Within ~3s trigger another ping - it should now play through the NEW default automatically (a ~3s poll re-binds the held stream). Switch the OS default back and confirm it follows again. If you instead pick a SPECIFIC device in-app, it should stay on that device and NOT follow OS changes.
