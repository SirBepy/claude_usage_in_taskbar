# Tomorrow's AI Prompt

Generated by: manual (/next-ai-prompt)
Generated: 2026-05-20 14:25 UTC
Branch: master

## Context

Daemon pivot: Phases 1-4 now shipped on master (Phase 4 = channels moved into the daemon, merged 2026-05-20). Channels are daemon-owned (spawn/kill/show-hide/autostart + RPC), app commands are thin RPC clients, and channel-spawned sessions tag Automated via inner-claude-pid matching (Windows `cmd.exe /C claude` wrapper handled). Automated tagging was verified live. Also did the de-risk ai_todos 59 + 61 (deleted dead app-side detector + channel lifecycle) and 62 (channel dedup + boot-time bridge adoption). master is ~24 commits ahead of origin and is HELD (unpushed) by Joe's choice. Next planned work is Phase 5.

## Verify checklist

- [ ] `git pull` (note: master is local-only/unpushed; nothing to pull unless Joe pushed)
- [ ] Verify ai_todo 62 adoption live (the one untested thing): start a daemon, let an ObsidianVault `--remote-control` bridge be running, restart the daemon, confirm the log shows `adopt_running_channels: adopting project ...` and it does NOT spawn a duplicate bridge. Kill app first (owns port 27182): `Get-Process claude-usage-tauri,cc-companion-daemon | Stop-Process -Force`.
- [ ] `cargo build --manifest-path src-tauri/Cargo.toml --bin cc-companion-daemon --bin claude-usage-tauri` → clean (3 warnings expected: CommandExt in daemon/lifecycle.rs is the only remaining one now).
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml --lib daemon:: ` → all pass.
- [ ] Channels e2e (optional, app must be closed): `cargo test --manifest-path src-tauri/Cargo.toml --test daemon_channels_e2e -- --ignored` → 1 pass.

## Open decisions

- Publish master? It's ~24 commits ahead of origin, held unpushed. Run `/commit pushnbump` when ready to release (triggers CI). Joe said "hold" 2026-05-20.
- Phase 5 scope/boundary: per precedent it's app-side feature flag (`experimental.useDaemon`) + rewriting the 9 `// TODO: Phase 5` chat-IPC registry-mutation stubs in `ipc/chat/{builtins,lifecycle,run}.rs` as daemon RPC calls. Confirm the flag default + whether channels stay flag-independent (they cut over unconditionally in Phase 4).

## Files changed

- `src-tauri/src/daemon/channels.rs` (new in Phase 4: daemon channel lifecycle + adopt_running_channels + dedup guard)
- `src-tauri/src/daemon/{methods,state,hooks_server,mod}.rs`, `src-tauri/src/daemon_client/mod.rs` (channel RPC + claude-pid correlation)
- `src-tauri/src/channels/{manager,spawn}.rs` (claude_pid field + resolve_claude_pid), `channels.rs` (dropped lifecycle), `channels/lifecycle.rs` DELETED
- `src-tauri/src/{state,lib}.rs` (cached_channels, channels_changed handler, removed dead spawns)
- `src-tauri/src/ipc/channels.rs` (RPC clients), `src-tauri/src/sessions/{registry,detector}.rs` (set_kind/retag_pid_as_automated; deleted dead run)
- `src-tauri/Cargo.toml` (default-run), `src-tauri/tests/daemon_channels_e2e.rs` (new)

## Suggested next steps

- Start Phase 5 in this fresh chat: read spec `docs/superpowers/specs/2026-05-19-detached-daemon-design.md` (gitignored, on disk) + the Phase 1-3 plan docs for shape, then run writing-plans for Phase 5. Locked execution pattern: subagent-driven, uninterrupted, feature branch per phase, ff-merge, branch auto-delete. Subagents stage-only; main agent runs `/commit` between tasks.
- Before or during Phase 5, do the live verify of ai_todo 62 adoption (above).
- Remaining deferred Phase 3/4 ai_todos (low priority): 57 (shared pid-is-live helper), 58 (split daemon hooks_server by route), 63 (app fetch instance snapshot on connect - fixes empty Running Instances on app launch).
