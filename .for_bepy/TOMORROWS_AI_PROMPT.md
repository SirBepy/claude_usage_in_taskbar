# Tomorrow's AI Prompt

Generated by: /close
Generated: 2026-05-12 00:25 UTC
Branch: master

## Context

Shipped skill-token-tracking end-to-end (Stop hook → Rust transcript parser → daily JSONL store under `<app-data>/skill-usage/` → Statistics widget + per-skill detail view) plus a Skills page listing every installed skill from `~/.claude/skills/` + plugin caches with search. Fixed the v1 multi-skill-turn attribution limitation: when a single assistant turn issues N Skill calls, usage is now split proportionally by each skill's `tool_result` body length, with remainder-to-last so per-skill totals exactly match the original turn usage. Two version bumps (0.1.46, 0.1.47); the hook installer is at v3 and will re-register existing users for the new Stop hook on next app launch. Manual end-to-end UI verification is still owed.

## Verify checklist

- [ ] `git pull`
- [ ] Launch the app (relaunch will trigger `migrate_hook_install_if_needed` → installs new Stop hook entry in `~/.claude/settings.json`)
- [ ] In a separate terminal, run a Claude Code session, type `/commit` once, finish the turn, exit the session. Confirm `<app-data>/skill-usage/events-YYYY-MM-DD.jsonl` gets a row with `source:"manual"`.
- [ ] Open Dashboard → Statistics. Pie + table widget under "Skills (last 7 days)" should populate. Sort columns by clicking headers.
- [ ] Click a skill row → skill detail view loads with counters, source pie, invocations table. Confirm `manual` badge is blue on the `/commit` row.
- [ ] Sidemenu → Skills. Search filter on. Confirm every skill in `~/.claude/skills/` and every plugin skill shows up.
- [ ] If anything wrong, check `<app-data>/logs/claude-usage-tauri.log` for `/hooks/stop` lines.

## Open decisions

- None blocking. The 4 ai_todos (43-46) are DRY cleanup, take them at any pace.

## Files changed

- `src-tauri/src/skill_usage/{mod,types,parser,store}.rs` — new module
- `src-tauri/src/ipc/skill_usage.rs` — new IPC commands
- `src-tauri/src/ipc.rs`, `src-tauri/src/lib.rs` — register module + IPC
- `src-tauri/src/hooks/server.rs` — new `on_stop` handler + `/hooks/stop` route
- `src-tauri/src/hooks/installer.rs` — bumped `CURRENT_INSTALL_VERSION` to 3, merges Stop
- `src-tauri/src/settings/paths.rs` — `skill_usage_dir()`
- `src-tauri/tests/{skill_usage_parser,skill_usage_store,skill_usage_endpoint}.rs` — 12 new tests + fixtures
- `src-tauri/tests/{export_types,hook_installer_merge}.rs` — registered types + Stop assertions
- `src/views/statistics/{skill-usage-widget.ts,skill-usage-widget.css}` — pie + sortable table
- `src/views/skill-detail/{skill-detail.ts,skill-detail.css}` — new view
- `src/views/skills/{skills.ts,skills.css}` — new view with search
- `src/views/statistics/statistics.ts` — mount widget
- `src/shared/api.ts` — 3 new methods + 1 event listener
- `src/main.ts`, `src/index.html` — register routes + sidemenu entry

## Suggested next steps

- After E2E verification clears, pick up `ai_todos/43-46` (small DRY cleanups around `fmtTokens`, `tokenTotal`, pie SVG math, `basename`).
- Consider exposing skill-usage data on the Dashboard (intentionally deferred from v1; spec listed it as future work).
- Add a backfill action to scan historical transcripts and rebuild `skill-usage/` data for sessions that happened before the Stop hook was installed (currently the feature starts from zero).
