# Tomorrow's AI Prompt

Generated by: /autopilot re-source run (via /next-ai-prompt --mode close)
Generated: 2026-06-06 03:55 UTC
Branch: master

## Context

Re-sourced the 1136 clips that the old 2.0s guillotine trim cropped mid-word, replacing each in place with a FULL-length original (same filename, slots stay valid). 1054/1136 fixed (92.8%); 113 of those are 5-10s (need ear approval); 82 left as the old crop (no unattended source). HotS (876), sims (111), wc3 (17), spongebob (10) fully or near-fully done; family-guy (18/61) and simpsons (10/38) are the low-yield tail. Audio lives OUTSIDE the repo (`%APPDATA%\claude-usage-tauri\characters\`); only two .for_bepy handoff sidecars were committed (238b84a).

## Verify checklist

- [ ] `git pull`
- [ ] Relaunch Claude Usage app, open Characters view, Refresh. Play a few HotS clips (e.g. alarak, abathur, sonya) - should be full lines, not 2s mid-word chops.
- [ ] Skim `.for_bepy/resource_pending_5to10s.txt` (113 clips), audition any that feel long; tell Claude "find a shorter <char> <action> line" to swap.
- [ ] Spot-check a sims clip + a wc3 clip (acolyte) play as full lines.

## Open decisions

- Approve/reject the 113 5-10s clips by ear (`.for_bepy/resource_pending_5to10s.txt`).
- Family-guy: a looser-phrase-match pass could recover ~6-8 more Stewie/Quagmire lines (declined this run to avoid shipping semantically-wrong clips). Want it?
- The 82 unresolved (`.for_bepy/resource_unresolved.txt`) - decide whether to hand-source manually or accept the crops.
- Thin-character (<4 slot) deletion pass was deferred by you to a later run - still pending.

## Files changed

- `.for_bepy/resource_pending_5to10s.txt` (new - 113 pending-approval paths)
- `.for_bepy/resource_unresolved.txt` (new - 82 unresolved paths)
- (out-of-repo) 1054 character audio files replaced in place under `%APPDATA%\claude-usage-tauri\characters\`

## Suggested next steps

- Looser-match family-guy pass (realmofdarkness catalog) if you want the ~6-8 more lines.
- Re-source the 28 simpsons + 43 family-guy unresolved via a different source, or accept and move on.
- The 7 HotS `laugh` clips have no jamiephan/fandom source - low priority (pool clips, unslotted).
- Resume the deferred thin-character cleanup pass.
