# Tomorrow's AI Prompt

Generated by: manual (/next-ai-prompt, accounts-UI handoff)
Generated: 2026-07-08 14:45 UTC
Branch: master

## Context

Multi-account wizard reworked to browser-first this session (commits 14cf1451..71834c0c): browser login supplies identity + cookie, CLI /login only when the profile dir lacks credentials, X-close with discard confirm, in-app askConfirm replacing the broken dialog-plugin confirm, drift false-positive fixed (missing oauthAccount + valid credentials is a complete login). All unit suites green; wdio e2e harness is environmentally broken (ai_todo 169). Next session is a pure UI polish pass on Settings > Accounts.

## Verify checklist

- [ ] `git pull`
- [ ] `cargo tauri dev` (via /supervised-run), Settings > Accounts: "personal" row renders with NO red warning
- [ ] Add account wizard: steps read Create > Browser login > CLI login > Finalize; backdrop click does nothing; X past step 1 asks to discard
- [ ] Remove account: in-app confirm dialog actually appears and blocks

## Open decisions

- None blocking. UI taste calls below are Joe's; propose via mockup first.

## Files changed

- src-tauri/src/ipc/accounts.rs, src-tauri/src/accounts/{drift,login_step}.rs, src-tauri/src/lib.rs (browser-first flow, drift fix)
- src-tauri/src/scraping/client.rs (fetch_web_account /api/account)
- src/views/settings/subviews/accounts/{add-account-wizard.ts,.css,accounts.ts,wizard-logic.ts}
- src/shared/{confirm.ts,confirm.css,api.ts}
- tests/{add-account-wizard-dom,confirm,add-account-wizard-logic}.test.mjs, e2e/specs/multi-account.e2e.js
- docs/multi-account/01-account-identity.md

## Suggested next steps

THE TASK: redesign the account row/card on Settings > Accounts (`src/views/settings/subviews/accounts/accounts.ts` + `accounts.css`). Joe's asks, verbatim intent:

1. Account colour as a LEFT border on the row, not the current top border.
2. Collapse the button pile in the row (star/refresh/Log out/Remove and the projects-row buttons) behind a "more options" (kebab/ellipsis) button.
3. Add an Edit button.
4. Edit opens options to: rename the account (label), change colour, change icon, and - most important - ADD/REMOVE PROJECTS bound to the account. (Project binding IPC already exists: see `.acc-btn-add-project` / remove-binding handlers in accounts.ts and the reverse-projects panel. Icon/colour pickers already exist in the wizard finalize step - reuse those components/styles, do not duplicate: `icon-grid`/`icon-tile` + `swatches` incl. the custom-colour swatch in add-account-wizard.ts/css. A rename/recolour IPC may need adding backend-side - check `accounts_store` for an update path first.)

PROCESS REQUIREMENTS (Joe was explicit):
- Preview BEFORE building: Joe wants to see what it will look like first. Use the local `/brainstorm` skill (never superpowers:brainstorming) and the visual-companion approach from memory: build a throwaway HTML mockup of the proposed row/edit-UI and OPEN IT IN THE BROWSER for Joe (memory: "show preview = browser companion"; "Visual companion trigger - only for layout/mockup/UI-placement questions"). Iterate on the mockup until he approves, THEN implement.
- Prior visual spec exists at `.for_bepy/multi-account-mockup.html` (add-account-wizard.css cites it) - extend that style language.
- Phosphor icons only. UX taste = Joe's; give a recommendation but let him pick.

After the UI pass lands: `/commit pushnbump` to ship v0.2.9 (Joe already wants a release once the accounts area feels right). Also open: ai_todo 168 (migrate remaining confirm() sites), ai_todo 169 (wdio WebView2 session creation).
