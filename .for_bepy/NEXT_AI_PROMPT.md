# Tomorrow's AI Prompt

Generated by: manual (/next-ai-prompt)
Generated: 2026-06-29 14:29 UTC
Branch: master

## Context

Session fixed the post-rename data loss (characters + full history restored from the old
`claude-usage-tauri` dir; added a one-shot migration). Also fixed a latent boot render-gate
bug + tests, and updated stale tests. The MAIN open item: a **white "ghost" dashboard window
appears on startup** - fully diagnosed but NOT yet fixed. The complete brief is in
**`.for_bepy/ai_todos/143-dashboard-white-ghost-window-on-startup.md`** - READ THAT FIRST; it
has the root cause, the usage-polling constraint, what was already tried/ruled out, the
proposed fix, and the screenshot-based verify loop. Handing off because this session's context
got bloated with dead-ends + screenshots; a fresh context will implement the fix more cleanly.

## Verify checklist

- [ ] `git pull`
- [ ] Read `.for_bepy/ai_todos/143-dashboard-white-ghost-window-on-startup.md` end to end before touching code.
- [ ] Confirm the repro: start the app (supervised dev or a release build) and screen-capture (NOT window APIs - they lie here) to see the white `Claude Conductor` window.
- [ ] Verify the data fix held: characters + usage history still present in the app (`%APPDATA%\claude-conductor`).
- [ ] `npx vitest run --pool=forks --poolOptions.forks.minForks=1 --poolOptions.forks.maxForks=4` is green (440+ pass).

## Open decisions

- Fix approach for the ghost window (ai_todo 143): build the main window in code (keep it
  created-at-startup + hidden so usage polling still runs) vs. first investigating exactly
  what shows the config window (try disabling tauri-plugin-window-state and screenshot). Lead
  with the cheap window-state test; it may pinpoint a smaller fix.

## Files changed

- `src-tauri/src/settings/paths.rs`, `src-tauri/src/lib.rs` - pre-rename data-dir migration (87ad555)
- `src/shared/boot.ts`, `src/shared/initial-render-gate.ts`, `tests/boot-render-gate.test.mjs` - render-gate fix + regression test (2dab1f1, 1d238fb)
- `tests/sessions-helpers.test.mjs`, `tests/remote-gate.test.mjs`, `tests/transport-http.test.mjs`, `tests/chat-renderer-custom-chips.test.mjs` - stale-test updates (1f0e102)
- `.for_bepy/ai_todos/143-dashboard-white-ghost-window-on-startup.md` - ghost-window diagnosis (8dab202)

## Suggested next steps

- Implement the ghost-window fix per ai_todo 143. Verify with screen-pixel capture (PowerShell
  `System.Drawing` CopyFromScreen -> PNG -> view it), NOT window-enumeration APIs.
- Use the supervisor to restart + read logs headless: `POST /procs/claude-usage-in-taskbar:cargo-tauri/restart`
  and `GET .../logs` on `127.0.0.1:<api_port>` (token in `%APPDATA%\com.sirbepy.server-supervisor\supervisor\api_token.txt`).
- Only commit the fix once a screenshot proves the white window is gone AND tray usage still updates (polling intact).
</content>
