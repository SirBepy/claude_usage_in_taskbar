# Tomorrow's AI Prompt

Generated by: /close (manual, end of /batch-todos + /autopilot + chip-fix session)
Generated: 2026-06-15 18:30 local
Branch: master

## Context

Big session, all SHIPPED + DEPLOYED at **v0.1.91** (CI green, run on 8a5554f). Cleared every batchable EASY ai_todo (94, 98, 101 x2, 107, 108, 110), fixed held-message auto-send (sends on its own when you stage-while-busy then just wait), built the project tech-logo face (ai_todo 99), and fixed the working-bar chip highlight (was lighting EVERY in-flight chip; now only the current-activity chip pulses and moves with the activity). A CONCURRENT autopilot agent was also on master this session (commits ba95f69 + bcb3f08) - it parked ai_todos 85 + 90 with investigation findings, and its 99 commit was just a doc/todo-deletion bundle, not duplicate code (verified: my 82b29d7 is the sole 99 impl).

## Verify checklist

- [ ] `git pull`
- [ ] Let the app auto-update to 0.1.91, then confirm chat send works again (it broke mid-session purely because my cargo builds disrupted the running dev daemon - should be fine on the new build; if not, restart the daemon = known pipe-wedge clear).
- [ ] Eyeball project tech-logos (ai_todo 99): projects with no custom emoji/image avatar show their real icon file OR a Devicon stack logo OR a generic folder, in both the projects list AND the project-detail header. Custom avatars unchanged. Devicon loads from unpkg (needs internet on first paint).
- [ ] Eyeball the chip highlight: while a chat works, only ONE chip pulses (the current action, e.g. File Changes while "Editing x.ts"), and it moves as the activity moves - not the whole strip.
- [ ] Confirm held-message auto-send: send while busy, then DON'T touch the composer; it sends on its own within ~2s when the turn ends (still holds if the turn ends on a question).

## Open decisions

- None blocking. Optional: ai_todo 99 left the secondary avatar sites (subview-header, session-detail, character-pick) on the old "?" placeholder for the no-avatar case - extend tech-logos there too, or leave them? (logged in COMMENTS_FOR_BEPY.md).

## Files changed

- Backend: `src-tauri/src/ipc/project_icons.rs` (new), `ipc.rs`, `lib.rs`, `ipc/misc.rs` (split into `ipc/models.rs` + `ipc/window.rs`; chrono date), `ipc/models.rs` (new), `ipc/window.rs` (new)
- Frontend: `src/shared/chat/held-messages.ts` (auto-send timer), `src/shared/chat/chat-renderer.ts` (chip highlight), `src/shared/projects.ts` + `src/shared/api.ts` + `src/index.html` + `src/styles/widgets.css` (tech logos), `src/shared/character-icon.ts` (new, dedup), `src/views/sessions/session-statusbar.ts`, `src/views/sessions/view-more-menu.ts` (new), `src/views/sessions/sessions.ts`, `src/views/sessions/sidebar.ts` + `active-session.ts` (projBadgeHtml dedup), `src/views/projects/projects.ts`, `src/views/project-detail/project-detail.ts`
- Tests: `tests/held-messages.test.mjs`, `tests/chat-renderer-activity.test.mjs`, `tests/chat-renderer-custom-chips.test.mjs`

## Suggested next steps

- **ai_todo 90 (held-messages e2e)** - now extra relevant: the held-messages controller changed this session (656f58d added a self-retry timer for the "stage then wait" path). The e2e should cover that exact flow. Parked with findings by the concurrent agent (bcb3f08) - read the ai_todo file first.
- **ai_todo 85 (question-status lost on chat open/switch)** - parked with investigation findings (bcb3f08); ready to implement.
- **ai_todo 81 (tauri-kit cleanup)** - would also fix the long-standing pre-existing `vendor/tauri_kit/frontend/settings/stack.ts(47,36) TS2532` error that fails `pnpm tsc --noEmit` every run (it's the ONLY tsc error; everything else is clean).
- Concurrent autopilot agents run on master - ALWAYS `git add <file>` by name, never `git add -A`, and commit each chunk fast to shrink the collision window.
